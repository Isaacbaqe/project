import { MathematicsPipe } from './mathematics.pipe';

describe('MathematicsPipe', () => {
  it('create an instance', () => {
    const pipe = new MathematicsPipe();
    expect(pipe).toBeTruthy();
  });
});
